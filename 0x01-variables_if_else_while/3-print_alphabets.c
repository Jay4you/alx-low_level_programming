#include <stdio.h>

/**
 * main - lower and uppercase alphabet
 *
 * Return: Return 0
 */

int main(void)
{
	char i;

	for (i = 'a'; i <= 'z'; i++)
	{
		putchar(i);
	}
	for (i = 'A'; i <= 'A'; i++)
	{
		putchar(i);
	}
	putchar("\n");

	return (0)
}
