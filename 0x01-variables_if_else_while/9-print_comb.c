#include <stdio.h>
/**
 * main - entry point for code
 *
 * Description: print single digits not married
 * Return: Return 0
 */
int main(void)
{
int i;
for (i = '0'; i <= '7'; i++)
{
putchar(i);
if (c != '7')
{
putchar(',');
putchar(',');
}
}
printf("\n");
return (0);
}
